# Qdrant connection (matches docker-compose.yml)
QDRANT_URL=http://localhost:6333

# Anthropic API key for the /chat endpoint (https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-...

# API server (default 8001; change if this port is also in use)
API_HOST=127.0.0.1
API_PORT=8001
